<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title><%= name %> — Portfolio</title>

    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap"
      rel="stylesheet"
    />

    <style>
      :root {
        --bg: #f7f8fb;
        --paper: #ffffff;
        --muted: #6b7280;
        --accent: #6c8cff;
        --accent-2: #f08fb3;
        --soft: #eef2ff;
        --shadow: 0 10px 30px rgba(20, 30, 80, 0.06);
        --radius: 14px;
      }

      * {
        box-sizing: border-box;
      }
      html,
      body {
        height: 100%;
        margin: 0;
        background: var(--bg);
        font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto,
          Helvetica, Arial;
        color: #0f172a;
        -webkit-font-smoothing: antialiased;
      }
      a {
        color: inherit;
      }

      .wrap {
        max-width: 1100px;
        margin: 28px auto;
        padding: 20px;
      }

      /* subtle global reveal */
      @keyframes floatIn {
        from {
          opacity: 0;
          transform: translateY(18px) scale(0.995);
        }
        to {
          opacity: 1;
          transform: translateY(0) scale(1);
        }
      }

      header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 12px;
        margin-bottom: 18px;
      }
      .brand {
        display: flex;
        align-items: center;
        gap: 12px;
      }
      .logo-mark {
        width: 44px;
        height: 44px;
        border-radius: 10px;
        background: linear-gradient(135deg, var(--accent), var(--accent-2));
        box-shadow: 0 8px 30px rgba(108, 140, 255, 0.12);
      }
      .site-title {
        margin: 0;
        font-weight: 700;
        font-size: 18px;
        color: #0b1220;
      }
      .header-actions {
        display: flex;
        gap: 10px;
        align-items: center;
      }

      .btn {
        display: inline-block;
        padding: 10px 14px;
        border-radius: 10px;
        text-decoration: none;
        font-weight: 700;
        color: white;
        background: linear-gradient(90deg, var(--accent), #5aa0ff);
        transition: transform 0.18s ease, box-shadow 0.18s ease;
      }
      .btn.ghost {
        background: transparent;
        color: var(--accent);
        border: 1px solid rgba(15, 41, 100, 0.06);
      }
      .btn:hover {
        transform: translateY(-4px);
        box-shadow: 0 12px 30px rgba(16, 40, 120, 0.08);
      }

      /* HERO */
      .hero {
        display: flex;
        gap: 28px;
        align-items: center;
        flex-wrap: wrap;
      }
      .hero-main {
        flex: 1;
        min-width: 260px;
        animation: floatIn 0.6s ease both;
      }
      .greeting {
        font-size: 34px;
        line-height: 1.02;
        margin: 0;
        font-weight: 800;
        color: #07103a;
      }
      .role {
        margin: 8px 0;
        color: var(--muted);
        font-weight: 600;
      }
      .quote {
        margin-top: 6px;
        color: #334155;
        font-style: italic;
      }

      .hero-side {
        width: 300px;
        animation: floatIn 0.9s ease both;
      }
      .profile-card {
        background: var(--paper);
        border-radius: 16px;
        padding: 8px;
        display: flex;
        align-items: center;
        justify-content: center;
        box-shadow: var(--shadow);
        transition: transform 0.28s ease, box-shadow 0.28s ease;
      }
      .profile-card:hover {
        transform: translateY(-6px);
        box-shadow: 0 18px 50px rgba(15, 30, 80, 0.08);
      }
      .profile-img {
        width: 280px;
        height: 280px;
        object-fit: cover;
        border-radius: 12px;
        display: block;
      }

      /* layout */
      .grid {
        display: grid;
        grid-template-columns: 1fr 340px;
        gap: 22px;
        margin-top: 22px;
      }
      @media (max-width: 980px) {
        .grid {
          grid-template-columns: 1fr;
        }
        .hero-side {
          width: 100%;
        }
        .profile-img {
          width: 100%;
          height: 260px;
        }
      }

      .card {
        background: var(--paper);
        border-radius: var(--radius);
        padding: 18px;
        box-shadow: var(--shadow);
        animation: floatIn 0.65s ease both;
      }
      .section-title {
        margin: 0 0 12px 0;
        font-weight: 700;
        color: #07103a;
        font-size: 18px;
      }

      /* about */
      .about p {
        color: #334155;
        margin: 0;
        line-height: 1.6;
      }

      /* chips */
      .chips {
        display: flex;
        gap: 10px;
        flex-wrap: wrap;
        margin-top: 10px;
      }
      .chip {
        background: var(--soft);
        padding: 8px 12px;
        border-radius: 999px;
        color: #1e293b;
        font-weight: 700;
        font-size: 13px;
        box-shadow: 0 6px 18px rgba(16, 40, 120, 0.03);
        transition: transform 0.18s;
      }
      .chip:hover {
        transform: translateY(-5px);
      }

      /* projects */
      .projects-grid {
        display: grid;
        gap: 14px;
        grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
        margin-top: 12px;
      }
      .project {
        border-radius: 12px;
        overflow: hidden;
        background: linear-gradient(180deg, #ffffff, #fbfdff);
        border: 1px solid rgba(16, 40, 120, 0.03);
        transition: transform 0.25s ease, box-shadow 0.25s ease;
      }
      .project:hover {
        transform: translateY(-6px);
        box-shadow: 0 18px 45px rgba(16, 40, 120, 0.06);
      }
      .project-img {
        width: 100%;
        height: 150px;
        object-fit: cover;
        display: block;
      }
      .project-body {
        padding: 12px;
      }
      .project-body h4 {
        margin: 0 0 8px 0;
        color: #07103a;
      }
      .project-body p {
        margin: 0;
        color: var(--muted);
        font-size: 14px;
      }

      .proj-links a {
        text-decoration: none;
        color: var(--accent);
        font-weight: 700;
        margin-right: 10px;
      }

      /* sidebar */
      .sidebar .panel {
        margin-bottom: 12px;
      }
      .meta-row {
        display: flex;
        justify-content: space-between;
        gap: 8px;
        align-items: center;
      }
      .meta-label {
        color: var(--muted);
        font-weight: 600;
      }

      /* contact */
      .contact-list a {
        display: block;
        color: var(--accent);
        font-weight: 700;
        text-decoration: none;
        margin-bottom: 8px;
      }

      /* footer */
      footer {
        text-align: center;
        color: var(--muted);
        margin-top: 26px;
        font-size: 14px;
      }

      /* small decorative animations */
      .float-slow {
        animation: floatSlow 6s ease-in-out infinite;
      }
      @keyframes floatSlow {
        0% {
          transform: translateY(0);
        }
        50% {
          transform: translateY(-6px);
        }
        100% {
          transform: translateY(0);
        }
      }

      /* fade stagger applied with inline style delays (we'll add via attribute) */
      .stagger {
        opacity: 0;
        transform: translateY(14px);
        transition: opacity 0.6s ease,
          transform 0.6s cubic-bezier(0.2, 0.9, 0.2, 1);
      }
      .stagger.visible {
        opacity: 1;
        transform: translateY(0);
      }
    </style>
  </head>

  <body>
    <div class="wrap">
      <!-- header -->
      <header>
        <div class="brand">
          <div
            class="logo-mark"
            aria-hidden="true"
            style="
              width: 44px;
              height: 44px;
              border-radius: 10px;
              background: linear-gradient(
                135deg,
                var(--accent-2),
                var(--accent)
              );
              box-shadow: 0 10px 40px rgba(192, 81, 141, 0.08);
            "
          ></div>
          <div>
            <div class="site-title">Portfolio</div>
            <div style="font-weight: 700; color: #0b1220"><%= name %></div>
          </div>
        </div>

        <div class="header-actions">
          <a class="btn ghost" href="<%= downloadUrl %>" target="_blank"
            >Download CV</a
          >
          <a class="btn" href="<%= resumeUrl %>" target="_blank">Resume</a>
        </div>
      </header>

      <!-- hero -->
      <section class="hero">
        <div class="hero-main">
          <h1 class="greeting">
            Hello — I'm
            <span
              style="
                background: linear-gradient(
                  90deg,
                  var(--accent),
                  var(--accent-2)
                );
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
              "
              ><%= name %></span
            >
          </h1>
          <div class="role"><%= role || "Fullstack Developer" %></div>
          <div class="quote"><%= quote || "" %></div>

          <div class="card" style="margin-top: 18px">
            <h3 class="section-title">About</h3>
            <p><%= about || "No bio provided." %></p>

            <div style="margin-top: 12px">
              <h4
                style="
                  margin: 0 0 8px 0;
                  font-size: 14px;
                  color: #0b1220;
                  font-weight: 700;
                "
              >
                Interests
              </h4>
              <div class="chips">
                <% (interests || []).forEach(i => { %>
                <div class="chip"><%= i %></div>
                <% }) %> <% if(!(interests && interests.length)){ %>
                <div class="chip">—</div>
                <% } %>
              </div>
            </div>
          </div>
        </div>

        <aside class="hero-side">
          <div class="profile-card float-slow">
            <% if (profileImageUrl) { %>
            <img
              src="<%= profileImageUrl %>"
              alt="<%= name %>"
              class="profile-img"
            />
            <% } else { %>
            <div
              style="
                width: 100%;
                height: 100%;
                display: flex;
                align-items: center;
                justify-content: center;
                color: var(--muted);
              "
            >
              No photo
            </div>
            <% } %>
          </div>
        </aside>
      </section>

      <!-- main grid -->
      <div class="grid">
        <div>
          <section class="card stagger" data-delay="80">
            <h3 class="section-title">Projects</h3>
            <div class="projects-grid">
              <% (projects || []).forEach((p, idx) => { %>
              <article class="project stagger" data-delay="<%= 140 + idx*60 %>">
                <% if (p.imageUrl) { %>
                <img
                  src="<%= p.imageUrl %>"
                  alt="<%= p.title %>"
                  class="project-img"
                />
                <% } else { %>
                <div
                  style="
                    height: 150px;
                    display: flex;
                    align-items: center;
                    justify-content: center;
                    color: var(--muted);
                    background: #fbfbff;
                  "
                >
                  No image
                </div>
                <% } %>
                <div class="project-body">
                  <h4><%= p.title || 'Untitled' %></h4>
                  <p><%= p.description || '' %></p>
                  <div style="margin-top: 10px">
                    <% if (p.github) { %><a
                      class="proj-links"
                      href="<%= p.github %>"
                      target="_blank"
                      >GitHub</a
                    ><% } %> <% if (p.live) { %><a
                      class="proj-links"
                      href="<%= p.live %>"
                      target="_blank"
                      >Live</a
                    ><% } %>
                  </div>
                </div>
              </article>
              <% }) %>
            </div>
          </section>
        </div>

        <aside class="sidebar">
          <div class="panel card stagger" data-delay="260">
            <div class="meta-row">
              <div>
                <div class="meta-label">Role</div>
                <div style="font-weight: 700"><%= role || '-' %></div>
              </div>
              <div>
                <div class="meta-label">Qualification</div>
                <div style="font-weight: 700"><%= qualification || '-' %></div>
              </div>
            </div>
          </div>

          <div class="panel card stagger" data-delay="320">
            <h3 class="section-title">Skills</h3>
            <div class="chips">
              <% (skills || []).forEach(s => { %>
              <div class="chip"><%= s %></div>
              <% }) %> <% if(!(skills && skills.length)){ %>
              <div class="chip">—</div>
              <% } %>
            </div>
          </div>

          <div class="panel card stagger" data-delay="380">
            <h3 class="section-title">Contact</h3>
            <div style="margin-top: 8px" class="contact-list">
              <% if (contacts && contacts.length) { %> <% contacts.forEach(c =>
              { %>
              <a href="<%= c %>" target="_blank"><%= c %></a>
              <% }) %> <% } else { %>
              <div style="color: var(--muted)">No contact links added.</div>
              <% } %>
            </div>
          </div>

          <div class="panel card stagger" data-delay="420">
            <h3 class="section-title">Downloads</h3>
            <div style="display: flex; gap: 8px; margin-top: 8px">
              <a class="btn ghost" href="<%= resumeUrl %>" target="_blank"
                >View Resume</a
              >
              <a class="btn" href="<%= downloadUrl %>" target="_blank"
                >Download</a
              >
            </div>
          </div>
        </aside>
      </div>

      <footer>
        <%= footer || ("© " + new Date().getFullYear() + " " + name) %>
      </footer>
    </div>

    <script>
      // apply stagger reveal based on data-delay attributes
      (function () {
        const nodes = document.querySelectorAll(".stagger");
        nodes.forEach((node) => {
          const d = parseInt(node.getAttribute("data-delay") || "0", 10);
          setTimeout(() => node.classList.add("visible"), d);
        });

        // small intersection observer fallback (optional)
        if ("IntersectionObserver" in window) {
          const io = new IntersectionObserver(
            (entries) => {
              entries.forEach((e) => {
                if (e.isIntersecting) e.target.classList.add("visible");
              });
            },
            { threshold: 0.08 }
          );
          document.querySelectorAll(".stagger").forEach((n) => io.observe(n));
        }
      })();
    </script>
  </body>
</html>
