<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title><%= portfolio.name %> — Portfolio</title>
    <style>
      :root {
        --accent: #4f46e5;
        --bg: #0f172a;
        --card: #0b1220;
        color: #e6eef8;
      }
      * {
        box-sizing: border-box;
      }
      body {
        margin: 0;
        font-family: Inter, system-ui, Arial;
        background: linear-gradient(180deg, #071126 0%, #041026 100%);
        color: var(--card);
      }
      .container {
        max-width: 1000px;
        margin: 30px auto;
        padding: 20px;
      }
      .header {
        display: flex;
        gap: 20px;
        align-items: center;
      }
      .avatar {
        width: 110px;
        height: 110px;
        border-radius: 14px;
        overflow: hidden;
        flex: 0 0 110px;
        box-shadow: 0 8px 24px rgba(0, 0, 0, 0.6);
      }
      .avatar img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        display: block;
        transform: scale(0.98);
        transition: transform 0.6s ease;
      }
      .avatar img:hover {
        transform: scale(1.03);
      }
      .info h1 {
        margin: 0;
        font-size: 28px;
        color: #fff;
      }
      .info p {
        margin: 8px 0 0 0;
        opacity: 0.85;
      }
      .links {
        margin-top: 10px;
        display: flex;
        gap: 10px;
      }
      .btn {
        padding: 8px 12px;
        border-radius: 8px;
        background: var(--accent);
        color: white;
        text-decoration: none;
      }
      .section {
        margin-top: 26px;
        background: rgba(255, 255, 255, 0.03);
        padding: 18px;
        border-radius: 12px;
      }
      .projects {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
        gap: 14px;
        margin-top: 12px;
      }
      .proj {
        background: #071330;
        border-radius: 10px;
        overflow: hidden;
        transition: transform 0.3s ease, box-shadow 0.3s;
      }
      .proj:hover {
        transform: translateY(-6px);
        box-shadow: 0 12px 30px rgba(0, 0, 0, 0.6);
      }
      .proj img {
        width: 100%;
        height: 140px;
        object-fit: cover;
      }
      .proj .pbody {
        padding: 12px;
      }
      @media (max-width: 600px) {
        .header {
          flex-direction: column;
          align-items: flex-start;
        }
        .avatar {
          width: 90px;
          height: 90px;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <div class="avatar">
          <% if (portfolio.profileImageUrl) { %>
          <img src="<%= portfolio.profileImageUrl %>" alt="avatar" />
          <% } else { %>
          <img
            src="https://via.placeholder.com/300x300?text=Avatar"
            alt="avatar"
          />
          <% } %>
        </div>
        <div class="info">
          <h1><%= portfolio.name %></h1>
          <p><%= portfolio.qualification || '' %></p>
          <div class="links">
            <% if (portfolio.linkedin) { %>
            <a class="btn" href="<%= portfolio.linkedin %>" target="_blank"
              >LinkedIn</a
            >
            <% } %> <% if (portfolio.github) { %>
            <a class="btn" href="<%= portfolio.github %>" target="_blank"
              >GitHub</a
            >
            <% } %>
          </div>
        </div>
      </div>

      <div class="section">
        <h3>About</h3>
        <p><%= portfolio.about %></p>
      </div>

      <div class="section">
        <h3>Experience</h3>
        <p><%= portfolio.experience %></p>
      </div>

      <div class="section">
        <h3>Projects</h3>
        <div class="projects">
          <% (portfolio.projects || []).forEach(function(p){ %>
          <div class="proj">
            <% if (p.imageUrl) { %><img
              src="<%= p.imageUrl %>"
              alt="project"
            /><% } %>
            <div class="pbody">
              <strong><%= p.title %></strong>
              <p style="opacity: 0.9"><%= p.description %></p>
            </div>
          </div>
          <% }); %>
        </div>
      </div>

      <footer style="margin-top: 18px; opacity: 0.6">
        Generated by MyPortfolio • <%= new
        Date(portfolio.createdAt).toLocaleDateString() %>
      </footer>
    </div>
  </body>
</html>
